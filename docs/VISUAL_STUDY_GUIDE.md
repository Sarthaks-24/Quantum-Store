# Visual Study Guide - Quantum Store

**Perfect for visual learners! Study this 30 minutes before quiz** 🎨

---

## 🏗️ System Architecture (Bird's Eye View)

```
┌─────────────────────────────────────────────────────────────┐
│                        USER BROWSER                          │
│                                                              │
│  ┌────────────┐  ┌──────────┐  ┌────────┐  ┌──────────┐  │
│  │ Dashboard  │  │  Files   │  │ Upload │  │ Preview  │  │
│  │  (Stats)   │  │ (Browse) │  │  (D&D) │  │ (Zoom)   │  │
│  └────────────┘  └──────────┘  └────────┘  └──────────┘  │
│         │              │             │            │         │
│         └──────────────┴─────────────┴────────────┘         │
│                          │                                   │
│                    React Frontend                           │
└──────────────────────────┼──────────────────────────────────┘
                           │
                     HTTP/JSON
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                    FastAPI Backend                          │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐  │
│  │  app.py (17 Endpoints)                               │  │
│  │                                                       │  │
│  │  POST /upload → POST /upload/batch                   │  │
│  │  GET /files → GET /file/{id} → GET /file/{id}/preview│  │
│  │  POST /analyze/json|pdf|image|text|video             │  │
│  │  GET /groups → POST /groups/rebuild                  │  │
│  └─────────────────────────────────────────────────────┘  │
│                           │                                  │
│                           ▼                                  │
│  ┌─────────────────────────────────────────────────────┐  │
│  │  Classifier (40+ Categories)                         │  │
│  │                                                       │  │
│  │  Images: 15 types    PDFs: 9 types                  │  │
│  │  JSON: 5 types       Text: 6 types                  │  │
│  │  Video: 5 types                                      │  │
│  └─────────────────────────────────────────────────────┘  │
│                           │                                  │
│                           ▼                                  │
│  ┌────────┐  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐     │
│  │ Image  │  │ PDF  │  │ JSON │  │ Text │  │ Video│     │
│  │Processor│ │Processor││Processor││Processor││Processor│  │
│  │        │  │      │  │      │  │      │  │      │     │
│  │K-means │  │ OCR  │  │Stream│  │TF-IDF│  │ cv2  │     │
│  │ phash  │  │PyMuPDF│ │ijson │  │Flesch│  │meta  │     │
│  └────────┘  └──────┘  └──────┘  └──────┘  └──────┘     │
│                           │                                  │
│                           ▼                                  │
│  ┌─────────────────────────────────────────────────────┐  │
│  │  Storage (JSON Files)                                │  │
│  │                                                       │  │
│  │  data/raw/uploads/          → Original files        │  │
│  │  data/processed/metadata/   → Analysis JSON         │  │
│  │  data/cache/                → Indices (phash, tfidf)│  │
│  └─────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

---

## 📤 Upload Flow (Step-by-Step)

```
Step 1: User drops file
    │
    ▼
┌───────────────────────┐
│  Upload.js            │
│  - handleDrop()       │
│  - addFiles()         │
└───────┬───────────────┘
        │ FormData
        ▼
┌───────────────────────┐
│  POST /upload         │
│  (FastAPI endpoint)   │
└───────┬───────────────┘
        │
        ▼
Step 2: Save to disk
┌───────────────────────┐
│  save_upload_file()   │
│  → data/raw/uploads/  │
│     {id}.ext          │
└───────┬───────────────┘
        │
        ▼
Step 3: Detect type
┌───────────────────────┐
│  detect_file_type_    │
│  comprehensive()      │
│                       │
│  1. Magic bytes       │
│  2. Extension         │
│  3. MIME type         │
└───────┬───────────────┘
        │ "image", "pdf", "json", "text", "video"
        ▼
Step 4: Process
┌───────────────────────┐
│  if type == "image":  │
│    ImageProcessor     │
│  elif type == "pdf":  │
│    PDFProcessor       │
│  elif type == "json": │
│    JSONProcessor      │
│  ...                  │
└───────┬───────────────┘
        │ analysis = {colors, text, schema, ...}
        ▼
Step 5: Classify
┌───────────────────────┐
│  classifier.          │
│  classify_file()      │
│                       │
│  - Check heuristics   │
│  - Return category    │
└───────┬───────────────┘
        │ "images_screenshot", "pdfs_scanned", etc.
        ▼
Step 6: Save metadata
┌───────────────────────┐
│  store.save_metadata()│
│  → data/processed/    │
│     metadata/{id}.json│
└───────┬───────────────┘
        │
        ▼
Step 7: Update indices
┌───────────────────────┐
│  store.update_        │
│  phash_index()        │
│  store.update_        │
│  tfidf_index()        │
└───────┬───────────────┘
        │
        ▼
┌───────────────────────┐
│  Return to frontend   │
│  {id, filename, ...}  │
└───────────────────────┘
        │
        ▼
    Display success!
```

---

## 🔍 File Type Detection (3 Methods)

```
┌─────────────────────────────────────────────────────┐
│  Input: filename, mime_type, file_bytes (first 512) │
└─────────────┬───────────────────────────────────────┘
              │
              ▼
┌──────────────────────────────────────┐
│  METHOD 1: Magic Bytes (Most Reliable)│
│                                       │
│  %PDF       → pdf                     │
│  \xff\xd8\xff → jpeg                  │
│  \x89PNG    → png                     │
│  GIF87a     → gif                     │
│  { or [     → json                    │
└──────────────┬───────────────────────┘
               │ Found?
               ├─ Yes → Return type
               │
               ▼ No
┌──────────────────────────────────────┐
│  METHOD 2: File Extension             │
│                                       │
│  .json      → json                    │
│  .pdf       → pdf                     │
│  .jpg/.png  → image                   │
│  .txt/.md   → text                    │
│  .mp4/.avi  → video                   │
└──────────────┬───────────────────────┘
               │ Found?
               ├─ Yes → Return type
               │
               ▼ No
┌──────────────────────────────────────┐
│  METHOD 3: MIME Type                  │
│                                       │
│  application/pdf     → pdf            │
│  image/png           → image          │
│  text/plain          → text           │
│  video/mp4           → video          │
│  application/json    → json           │
└──────────────┬───────────────────────┘
               │
               ▼
         Return type
      (or "unknown")
```

---

## 🎨 Image Processor Flow

```
┌─────────────────┐
│  analyze()      │
│  Input: path    │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────┐
│  1. _get_basic_info()            │
│     ✓ Dimensions (1920 × 1080)  │
│     ✓ Format (PNG, JPEG)        │
│     ✓ EXIF (camera data)        │
│     ✓ Alpha channel             │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  2. _analyze_colors()            │
│     ✓ K-means clustering         │
│     ✓ 5 dominant colors          │
│     ✓ RGB + Hex + %              │
│                                  │
│  Example:                        │
│  #FF0000 (35.2%)                │
│  #00FF00 (25.1%)                │
│  #0000FF (20.3%)                │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  3. _analyze_quality()           │
│     ✓ Brightness (0-255)        │
│     ✓ Sharpness (Laplacian)     │
│     ✓ Edge density (Canny)      │
│                                  │
│  Example:                        │
│  brightness: 128.5              │
│  sharpness: 1250.3              │
│  edge_density: 0.42             │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  4. _calculate_phash()           │
│     ✓ Perceptual hash            │
│     ✓ 16-char hex string         │
│     ✓ For similarity             │
│                                  │
│  Example: "a1b2c3d4e5f6g7h8"   │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  5. _calculate_histogram()       │
│     ✓ RGB histograms             │
│     ✓ 16-bin per channel         │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  6. _categorize_image()          │
│     ✓ logo / screenshot /        │
│       photo / graphic            │
│     ✓ Heuristic rules            │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  7. Return complete analysis     │
│     {dimensions, colors,         │
│      quality, phash, category}   │
└─────────────────────────────────┘
```

---

## 📄 PDF Processor Flow

```
┌──────────────────┐
│  analyze()       │
│  Input: path     │
└────────┬─────────┘
         │
         ▼
┌─────────────────────────────────┐
│  1. Open with PyMuPDF (fitz)     │
│     doc = fitz.open(path)        │
│     page_count = len(doc)        │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  2. Extract text from all pages  │
│     for page in doc:             │
│       text += page.get_text()    │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  3. Check if scanned             │
│     is_scanned = len(text) < 30  │
└────────┬────────────────────────┘
         │
         ├─ No text? ──────────────┐
         │                         │
         ▼                         ▼
┌──────────────────┐   ┌──────────────────────┐
│  Normal text PDF  │   │  4. TRIGGER OCR       │
│  (skip OCR)       │   │     PaddleOCR         │
└──────────────────┘   │     Max 5 pages       │
                        │     2-5s per page     │
                        └──────────┬───────────┘
                                   │
                                   ▼
                        ┌──────────────────────┐
                        │  Render page as image│
                        │  Run OCR             │
                        │  Extract text        │
                        └──────────┬───────────┘
                                   │
         ┌─────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  5. Generate preview             │
│     Render page 1 as PNG         │
│     Resize to 400px width        │
│     Base64 encode                │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  6. Analyze for classification   │
│     - Count images               │
│     - Detect forms               │
│     - Calculate ratios           │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  7. Return analysis              │
│     {text, is_scanned, preview,  │
│      image_count, has_forms}     │
└─────────────────────────────────┘
```

---

## 📊 JSON Processor Flow (Streaming)

```
┌──────────────────┐
│  analyze()       │
│  Input: path     │
└────────┬─────────┘
         │
         ▼
┌─────────────────────────────────┐
│  1. Check file size              │
│     file_size = os.path.getsize()│
└────────┬────────────────────────┘
         │
         ├─ < 5MB ──────┐           ├─ > 5MB ──────┐
         │               │           │               │
         ▼               │           ▼               │
┌──────────────┐        │   ┌──────────────┐       │
│  REGULAR     │        │   │  STREAMING   │       │
│  PARSE       │        │   │  PARSE       │       │
└──────┬───────┘        │   └──────┬───────┘       │
       │                │          │                │
       ▼                │          ▼                │
┌──────────────┐        │   ┌──────────────────┐   │
│ json.load()  │        │   │ ijson.items()    │   │
│ Load entire  │        │   │ Stream items     │   │
│ file         │        │   │ one-by-one       │   │
└──────┬───────┘        │   └──────┬───────────┘   │
       │                │          │                │
       ▼                │          ▼                │
┌──────────────┐        │   ┌──────────────────┐   │
│ Process all  │        │   │ Only first 50    │   │
│ records      │        │   │ samples          │   │
└──────┬───────┘        │   │                  │   │
       │                │   │ Count up to      │   │
       │                │   │ 1000 for schema  │   │
       │                │   └──────┬───────────┘   │
       │                │          │                │
       └────────────────┴──────────┘                │
                        │                           │
                        ▼                           │
         ┌──────────────────────────────┐           │
         │  2. Detect schema             │           │
         │     - Infer field types        │           │
         │     - Track nullable fields    │           │
         └──────┬───────────────────────┘           │
                │                                    │
                ▼                                    │
         ┌──────────────────────────────┐           │
         │  3. Calculate statistics      │           │
         │     - Min, Max, Avg            │           │
         │     - From samples only        │           │
         └──────┬───────────────────────┘           │
                │                                    │
                ▼                                    │
         ┌──────────────────────────────┐           │
         │  4. Detect inconsistencies    │           │
         │     - Type mismatches          │           │
         │     - Null values              │           │
         └──────┬───────────────────────┘           │
                │                                    │
                ▼                                    │
         ┌──────────────────────────────┐           │
         │  5. Return analysis           │           │
         │     {schema, samples,          │           │
         │      statistics, record_count} │           │
         └────────────────────────────────┘           │
                                                     │
Memory:   200MB RAM                   2MB RAM ◄──────┘
          (for 100MB file)            (for 100MB file)
```

---

## 🧠 Classifier Decision Tree (Images)

```
                    ┌──────────────┐
                    │ Image File   │
                    └──────┬───────┘
                           │
                           ▼
              ┌────────────────────────┐
              │ Get analysis from      │
              │ ImageProcessor:        │
              │ - edge_density         │
              │ - brightness           │
              │ - colors               │
              │ - dimensions           │
              └────────┬───────────────┘
                       │
                       ▼
         ┌─────────────────────────────┐
         │ edge_density > 0.3 AND       │
         │ resolution in                │
         │ [(1920,1080), (1366,768)]?  │
         └─────────┬──────────┬────────┘
                   │          │
              YES  │          │ NO
                   │          │
                   ▼          ▼
         ┌──────────────┐    │
         │ SCREENSHOT   │    │
         │ 0.85 conf    │    │
         └──────────────┘    │
                             │
                             ▼
         ┌─────────────────────────────┐
         │ brightness > 200 AND         │
         │ mostly grayscale?            │
         └─────────┬──────────┬────────┘
                   │          │
              YES  │          │ NO
                   │          │
                   ▼          ▼
         ┌──────────────┐    │
         │ SCANNED DOC  │    │
         │ 0.80 conf    │    │
         └──────────────┘    │
                             │
                             ▼
         ┌─────────────────────────────┐
         │ Text overlay detected?       │
         └─────────┬──────────┬────────┘
                   │          │
              YES  │          │ NO
                   │          │
                   ▼          ▼
         ┌──────────────┐    │
         │ MEME         │    │
         │ 0.75 conf    │    │
         └──────────────┘    │
                             │
                             ▼
         ┌─────────────────────────────┐
         │ aspect_ratio < 1.0 AND       │
         │ dimensions suggest portrait? │
         └─────────┬──────────┬────────┘
                   │          │
              YES  │          │ NO
                   │          │
                   ▼          ▼
         ┌──────────────┐    │
         │ SELFIE       │    │
         │ 0.75 conf    │    │
         └──────────────┘    │
                             │
                             ▼
         ┌─────────────────────────────┐
         │ Smooth colors AND            │
         │ unrealistic hue?             │
         └─────────┬──────────┬────────┘
                   │          │
              YES  │          │ NO
                   │          │
                   ▼          ▼
         ┌──────────────┐    │
         │ AI GENERATED │    │
         │ 0.70 conf    │    │
         └──────────────┘    │
                             │
                             ▼
         ┌─────────────────────────────┐
         │ Default category based on    │
         │ basic_category from processor│
         └──────────────────────────────┘
```

---

## 📦 Storage Structure

```
data/
│
├── raw/
│   └── uploads/
│       ├── abc123.pdf           ← Original file
│       ├── def456.png
│       └── ghi789.json
│
├── processed/
│   │
│   ├── metadata/
│   │   ├── abc123.json          ← Analysis result
│   │   ├── def456.json          │  {
│   │   └── ghi789.json          │    "id": "abc123",
│   │                            │    "filename": "report.pdf",
│   │                            │    "file_type": "pdf",
│   │                            │    "content_category": "pdfs_text_document",
│   │                            │    "analysis": {...}
│   │                            │  }
│   │
│   ├── schemas/
│   │   └── schema_uuid.json     ← JSON schemas
│   │
│   └── groups/
│       ├── images_screenshot.json    ← Category index
│       ├── pdfs_text_document.json   │  ["abc123", "def456"]
│       └── json_nested.json
│
└── cache/
    ├── phash_index.json         ← Image similarity index
    │                            │  {
    │                            │    "def456": "a1b2c3d4e5f6g7h8"
    │                            │  }
    │
    └── tfidf_index.json         ← Text similarity index
                                 │  {
                                 │    "abc123": {
                                 │      "vector": [0.5, 0.3, ...]
                                 │    }
                                 │  }
```

---

## 🎨 Frontend Component Tree

```
┌─────────────────────────────────────────────┐
│  index.html                                  │
│                                              │
│  ├─ Layout                                   │
│  │  ├─ Sidebar                               │
│  │  │  ├─ Logo/Brand                         │
│  │  │  ├─ Navigation                         │
│  │  │  │  ├─ Dashboard (active highlight)    │
│  │  │  │  └─ Files                           │
│  │  │  └─ Upload Button                      │
│  │  │                                        │
│  │  └─ Content Area                          │
│  │     │                                     │
│  │     ├─ Route: /                           │
│  │     │  └─ Dashboard                       │
│  │     │     ├─ Stats Cards                  │
│  │     │     │  ├─ StatCard (Total Files)    │
│  │     │     │  ├─ StatCard (Total Storage)  │
│  │     │     │  └─ StatCard (File Types)     │
│  │     │     │                               │
│  │     │     └─ Recent Files                 │
│  │     │        └─ FileCard × 5              │
│  │     │                                     │
│  │     ├─ Route: /files                      │
│  │     │  └─ Files                           │
│  │     │     ├─ Search Bar (300ms debounce)  │
│  │     │     ├─ Filters                      │
│  │     │     │  ├─ Type (image/pdf/json...)  │
│  │     │     │  ├─ Date Range                │
│  │     │     │  └─ Size Range                │
│  │     │     │                               │
│  │     │     ├─ Sort (name/date/size)        │
│  │     │     │                               │
│  │     │     └─ File Grid                    │
│  │     │        └─ FileCard × N              │
│  │     │           (click opens preview)     │
│  │     │                                     │
│  │     └─ Route: /upload                     │
│  │        └─ Upload                          │
│  │           ├─ Drop Zone                    │
│  │           ├─ File List                    │
│  │           │  └─ Progress bars             │
│  │           └─ Results (success/error)      │
│  │                                           │
│  └─ PreviewModal (overlay)                   │
│     ├─ Header                                │
│     │  ├─ Filename                           │
│     │  ├─ Close button                       │
│     │  └─ Previous/Next                      │
│     │                                        │
│     ├─ Tabs                                  │
│     │  ├─ Preview                            │
│     │  │  ├─ Image (with zoom/pan)           │
│     │  │  ├─ Text (scrollable)               │
│     │  │  └─ ZoomControls                    │
│     │  │     ├─ Zoom In                      │
│     │  │     ├─ Zoom Out                     │
│     │  │     └─ Reset                        │
│     │  │                                     │
│     │  └─ Analytics                          │
│     │     ├─ Basic Info                      │
│     │     ├─ Analysis Data                   │
│     │     └─ Expandable Fields               │
│     │                                        │
│     └─ Footer (file navigation)              │
│                                              │
└──────────────────────────────────────────────┘
```

---

## 🔢 Key Thresholds & Numbers (MEMORIZE!)

```
┌─────────────────────────────────────────────────┐
│  FILE TYPE DETECTION                            │
├─────────────────────────────────────────────────┤
│  Magic bytes read:        512 bytes             │
│  Methods tried:           3 (magic→ext→MIME)    │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  IMAGE PROCESSING                               │
├─────────────────────────────────────────────────┤
│  K-means clusters:        5 colors              │
│  Histogram bins:          16 per channel (RGB)  │
│  Phash length:            16 hex chars          │
│  Edge density threshold:  0.3 (screenshot)      │
│  Brightness threshold:    200 (scanned)         │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  PDF PROCESSING                                 │
├─────────────────────────────────────────────────┤
│  OCR trigger:             < 30 chars            │
│  OCR page limit:          5 pages               │
│  Preview width:           400px                 │
│  OCR time per page:       2-5 seconds           │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  JSON PROCESSING                                │
├─────────────────────────────────────────────────┤
│  Streaming threshold:     5MB                   │
│  Sample size:             50 records            │
│  Analysis depth:          1000 records          │
│  Metadata size target:    200KB                 │
│  Nesting threshold (nested): ≥ 3 levels         │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  TEXT PROCESSING                                │
├─────────────────────────────────────────────────┤
│  Top keywords:            20                    │
│  TF-IDF max features:     100                   │
│  Stopwords filtered:      50+ common words      │
│  Readability range:       0-100 (Flesch)        │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  VIDEO PROCESSING                               │
├─────────────────────────────────────────────────┤
│  Portrait threshold:      aspect_ratio < 0.8    │
│  Short video:             < 60 seconds          │
│  Long video:              > 3600 seconds (1hr)  │
│  High FPS threshold:      ≥ 50 fps              │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  CLASSIFICATION                                 │
├─────────────────────────────────────────────────┤
│  Total categories:        40+                   │
│  Image categories:        15                    │
│  PDF categories:          9                     │
│  JSON categories:         5                     │
│  Text categories:         6                     │
│  Video categories:        5                     │
│  Confidence range:        0.0 - 1.0             │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  FRONTEND                                       │
├─────────────────────────────────────────────────┤
│  Search debounce:         300ms                 │
│  Recent files shown:      5                     │
│  Zoom range:              0.5× - 5.0×           │
│  Zoom step (wheel):       0.9× / 1.1×           │
│  API timeout (normal):    30 seconds            │
│  API timeout (upload):    60 seconds            │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  BACKEND                                        │
├─────────────────────────────────────────────────┤
│  Total endpoints:         17                    │
│  Upload limit:            1GB (1073741824 bytes)│
│  Ebook page threshold:    > 100 pages           │
│  Receipt page limit:      ≤ 2 pages             │
└─────────────────────────────────────────────────┘
```

---

## 🎯 Common Quiz Patterns

### Pattern 1: "What triggers X?"
```
Q: What triggers OCR?
A: text_length < 30 characters

Q: What triggers streaming?
A: file_size > 5MB

Q: What identifies screenshot?
A: edge_density > 0.3 + common resolution
```

### Pattern 2: "What library does X?"
```
Q: Image processing?
A: PIL, OpenCV, imagehash, scikit-learn

Q: PDF processing?
A: PyMuPDF (fitz), PaddleOCR

Q: JSON streaming?
A: ijson

Q: Text analysis?
A: scikit-learn (TF-IDF)
```

### Pattern 3: "How many X?"
```
Q: Categories total?
A: 40+

Q: Image categories?
A: 15

Q: API endpoints?
A: 17

Q: Dominant colors?
A: 5 (K-means)
```

### Pattern 4: "What is the flow?"
```
Q: Upload flow?
A: Upload → Save → Detect → Process → Classify → Save metadata → Index

Q: File type detection?
A: Magic bytes → Extension → MIME type
```

---

**Print this guide and use it for quick visual review!** 🖨️
